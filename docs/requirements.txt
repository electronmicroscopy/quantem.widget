# Docs-build dependencies for CI. The runner is CPU-only and does NOT execute
# notebooks (execute_notebooks: "off" in _config.yml) - the .ipynb files are
# pre-baked with GPU outputs + widget state and committed. So the build needs
# only Jupyter Book, never quantem.widget / torch / cupy.
jupyter-book>=1.0,<2
# API reference via static analysis (astroid) - never imports the package.
sphinx-autodoc2>=0.5
